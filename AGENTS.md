# Agent instructions

Read `CLAUDE.md` first — it is the single source of project memory (product context, current
state, stack, commands, and rules). Then `WORKFLOW.md` for the git workflow, and
`BUILD_PLAN.md` for what to build next. Do not re-derive project context from scratch.
